<%= form_for(@task, remote: true) do |f| %>
  <%= f.text_field :task, placeholder: "New task...", class: "text" %>
  <%= f.hidden_field :task_list_id, value: @task_list_id.to_s %>
  <%= f.submit class: "button" %>
<% end %>
