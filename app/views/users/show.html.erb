<div class="list">
  <% if flash.any? %>
    <%= render 'layouts/flash' %>
  <% end %>

  <h3 class="title"><%= @user.name %>'s Task Lists:</h3>
  
  <div class="form">
    <%= render 'task_lists/form' %>
  </div>

  <div class="list_items">
    <%= render 'users/task_lists' %>
  </div>
</div>