<ul>
  <% if @task_lists %>
    <% @task_lists.each do |task_list| %>
      <li>
        <%= link_to task_list.name, task_list_path(task_list) %>
        <%= link_to "×", task_list_path(task_list), method: :delete, remote: true, class: "delete" unless @task_lists.length == 1 %>
      </li>
    <% end %>
  <% end %>
</ul>